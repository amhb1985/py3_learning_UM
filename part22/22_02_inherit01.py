#this is aabout the example in inherit:


#0. first of all we must set one class like this:

class Person:
    #0.1 then we define inint (naturally with self, name, and the year of birth)
    def __init__(self, name, year):
        
